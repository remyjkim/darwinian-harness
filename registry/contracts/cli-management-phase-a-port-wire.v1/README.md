# I321 Phase-A port-wire companion v1

Worker consumers vendor contract.json and projector.mjs unchanged. The wire is loopback-only: two ceremony observations stay in-process, twelve bearer-authenticated calls and one atomic cleanup use the master /v1/phase-a/* routes and exact named I321 Service Bindings. Normal cleanup carries the same Human bearer; controller fail-safe is generation-bound, erase-only, and forbids a Human bearer. Exactly two public files survive after normal cleanup or neither survives.
